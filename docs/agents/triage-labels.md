# 分诊标签

技能内部使用五个标准分诊角色。本文件将这些角色映射到本仓库问题追踪器中实际使用的标签字符串。

| 技能中的标签               | 我们追踪器中的标签     | 含义                       |
| -------------------------- | ---------------------- | -------------------------- |
| `needs-triage`             | `needs-triage`         | 维护者需要评估此问题       |
| `needs-info`               | `needs-info`           | 等待报告者补充更多信息     |
| `ready-for-agent`          | `ready-for-agent`      | 规格明确，AI agent 可接手  |
| `ready-for-human`          | `ready-for-human`      | 需要人工实现               |
| `wontfix`                  | `wontfix`              | 不会处理                   |

当技能提到某个角色（例如"应用 AFK-ready 分诊标签"）时，使用本表中对应的标签字符串。

可修改右侧列以匹配你实际使用的标签词汇。
